{% extends 'Juanber84ScheduleBundle::layout.html.twig' %}

{% block body -%}
    <h4 class="text book inline"><span class="icon folder-close color theme "></span> Projects creation</h4>   

    <form action="{{ path('admin_projects_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_projects') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
