{% extends 'Juanber84ScheduleBundle::layout.html.twig' %}

{% block body -%}
    <h1>Proyects creation</h1>

    <form action="{{ path('schedule_proyects_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('schedule_proyects') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
